---
type Props = {
  name: string;
};

const { name } = Astro.props;
---

<li
  class="bg-background border-border flex min-h-[64px] list-none items-center justify-center rounded-lg border p-4 text-center dark:bg-slate-800"
>
  <p class="text-foreground font-semibold">{name}</p>
</li>
