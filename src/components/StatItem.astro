---
export interface Props {
  value: string;
  label: string;
}

const { value, label } = Astro.props;
---

<div class="text-center">
  <div class="text-4xl md:text-5xl font-bold mb-2">{value}</div>
  <p class="text-teal-100">{label}</p>
</div>
