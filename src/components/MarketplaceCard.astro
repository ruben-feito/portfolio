---
export interface Props {
  name: string;
}

const { name } = Astro.props;
---

<div
  class="bg-background dark:bg-slate-800 rounded-lg p-4 border border-border text-center"
>
  <p class="font-semibold text-foreground">{name}</p>
</div>
